version: 2
machine:
  services:
    - docker

jobs:
  build:
    steps:
      - checkout
      - run:
          name: build docker image
          command: make build

deployment:
    branch: master
    commands:
      - make login
      - make push