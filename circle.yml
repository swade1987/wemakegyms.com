version: 2
defaults: &defaults
  working_directory: ~
  docker:
    - image: mugen/ubuntu-build-essential

jobs:
  build:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: build docker image
          command: make build

deployment:
    branch: master
    commands:
      - make login
      - make push